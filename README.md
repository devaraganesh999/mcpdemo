# To run this MCP server for add functionality add this into your mcp host/client config.

```json
{
  "adddemo": {
    "command": "uvx",
    "args": [
      "--from",
      "git+https://github.com/devaraganesh999/mcpdemo.git",
      "mcp-server"
    ]
  }
}
```
